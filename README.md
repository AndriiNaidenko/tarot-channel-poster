# Tarot Channel Poster Bot

🤖 Автоматический бот для публикации контента в Telegram канал @taro209

## 📋 Описание

Бот автоматически публикует посты в Telegram канал 4 раза в день:
- **09:00** - ✨ Энергия дня (астрология, луна)
- **14:00** - 🌌 Космос ↔️ 🔬 Наука (чередуется)
- **19:00** - 🤖 Технологии ↔️ 🌿 Природа (чередуется)
- **22:30** - 🌌 Космос (мистика ночи)

## 🚀 Быстрый старт

### 1. Настройка окружения

```bash
cp .env.example .env
```

Отредактируй `.env` файл:
```env
TELEGRAM_BOT_TOKEN=8551518470:AAG6AbFJwSwqphvIu_xIDHQ4N0v2eO3mEkg
OPENAI_API_KEY=your_openai_key_here
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Запуск

```bash
python channel_poster.py
```

## 🌐 Деплой на Railway

1. Создай новый проект на Railway
2. Подключи этот GitHub репозиторий
3. Добавь переменные окружения:
   - `TELEGRAM_BOT_TOKEN`
   - `OPENAI_API_KEY`
4. Railway автоматически использует `railway.json` конфигурацию

## ⚙️ Требования

- Python 3.10+
- Бот должен быть **администратором** канала @taro209 с правами:
  - ✅ Post messages

## 📝 Структура проекта

```
.
├── channel_poster.py      # Главный файл бота
├── backend/
│   ├── config.py         # Конфигурация
│   └── channel/
│       ├── news_fetcher.py    # Получение новостей
│       └── post_generator.py  # Генерация постов
├── requirements.txt      # Python зависимости
├── railway.json         # Railway конфигурация
└── README.md           # Эта инструкция
```

## 🔧 Настройка расписания

Измени расписание в `channel_poster.py`:

```python
scheduler.add_job(
    lambda: create_and_post(fixed_topic='energy'),
    CronTrigger(hour=9, minute=0),  # Измени время здесь
    id='morning_post'
)
```

## 📊 Логи

Логи сохраняются в:
- Консоль (stdout)
- Файл: `/tmp/channel_poster.log`

## 🆘 Проблемы?

### Бот не публикует посты
1. Проверь, что бот добавлен в канал как администратор
2. Проверь права бота: Settings → Administrators
3. Убедись, что включено право "Post messages"

### Ошибки API
1. Проверь токен бота
2. Проверь OpenAI API ключ
3. Проверь логи: `tail -f /tmp/channel_poster.log`

## 📄 Лицензия

MIT
